import "pkg:/source/lib/Utils.bs"

namespace models
  @node("Item", "ContentNode")
  class Item

    public json as sc.types.assocarray

    public version = 0
    public type = ""
    public isSerializable = true

    public function toString() as string
      return "<Item> (" + m.top.id + ") - '" + sc.getPath(m.json, "title", "NO TITLE") + "'"
    end function

    '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
    '++ Serialization support
    '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

    public function serialize() as sc.types.assocarray
      return {
        type: m.type
        json: m.json
      }
    end function

    public function deserialize(data as sc.types.assocarray)
      m.json = data.json
      m.type = data.type
    end function

    '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
    '++ Cloning
    '+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

    protected function cloneCustomFieldsIntoNode(node as sc.types.node)
      node.isSerializable = true
      node.type = m.top.type
    end function

  end class
end namespace
